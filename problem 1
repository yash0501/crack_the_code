#include <stdio.h>
int main(){
    int arr[100];
    int n,i,j;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    for(i=0;i<n;i++)
    {
        if(arr[i]<0)
        continue;
        else
        break;
    }
    printf("%d",arr[i]);
	return 0;
}
